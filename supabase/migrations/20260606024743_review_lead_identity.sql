-- CORRECTED: reviews.lead_id and idx_reviews_lead_id_unique were already
-- defined in the initial migration (20260527100000, lines 190 and 200-202).
-- This migration is a no-op against the current schema — preserved for
-- migration history continuity.
SELECT 1;
